services:
  my_server:
    build: ./
    networks:
      - app_net

  nginx_proxy:
    image: nginx:alpine
    ports:
      - "80:8080"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - my_server
    networks:
      - app_net

networks:
  app_net:

